// import React from 'react'; // React 17+ with JSX transform doesn't require this
import Register from './Register';

// RegisterPage is a wrapper around the main Register component
export default function RegisterPage() {
  return <Register />;
}
